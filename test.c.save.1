#include <iostream>

int main() {
	bool numbers[20] = {}; //true if dividebale
	for(int i = 1; i < sizeof(numbers); i++) {
		bool dividable = false;
		for(int j = 2; j < i-1; j++) {
			std::cout << i << "%" << j << "=" << i%j << "\n";
			if(i % j == 0) {
				dividable == true;
				break;
			}
	
	}
	//computing done

	for(int i = 1; i < sizeof(numbers); i++) {
		if(!numbers[i]) {
			std::cout << i << "\n";
		}
	}
}
